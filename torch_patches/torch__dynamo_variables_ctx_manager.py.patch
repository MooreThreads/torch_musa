diff --git a/torch/_dynamo/variables/ctx_manager.py b/torch/_dynamo/variables/ctx_manager.py
index 7ced6bd665b..57797df6963 100644
--- a/torch/_dynamo/variables/ctx_manager.py
+++ b/torch/_dynamo/variables/ctx_manager.py
@@ -932,10 +932,18 @@ class AutocastModeVariable(ContextWrappingVariable):
         args: Sequence[Any],
         kwargs: dict[str, Any],
     ) -> "AutocastModeVariable":
+        custom_backend = torch._C._get_privateuse1_backend_name()
+        custom_backend_mod = getattr(torch, custom_backend, None)
+        custom_autocast_list = []
+        if custom_backend_mod is not None and hasattr(custom_backend_mod, "amp"):
+            custom_autocast = getattr(custom_backend_mod.amp, "autocast", None)
+            if custom_autocast is not None:
+                custom_autocast_list.append(custom_autocast)
         assert func in [
             torch.amp.autocast_mode.autocast,
             torch.cuda.amp.autocast,
             torch.cpu.amp.autocast,
+            *custom_autocast_list,
         ]
         # device_type : str,
         # dtype : Optional[_dtype] = None,
@@ -950,9 +958,13 @@ class AutocastModeVariable(ContextWrappingVariable):
             if key == "device_type" and func in [
                 torch.cuda.amp.autocast,
                 torch.cpu.amp.autocast,
+                *custom_autocast_list,
             ]:
-                # pyrefly: ignore [unnecessary-comparison]
-                arg = "cuda" if func is torch.cuda.amp.autocast else "cpu"
+                if func in custom_autocast_list:
+                    arg = custom_backend
+                else:
+                    # pyrefly: ignore [unnecessary-comparison]
+                    arg = "cuda" if func is torch.cuda.amp.autocast else "cpu"
             else:
                 arg = bound_args.arguments[key]
             if isinstance(arg, VariableTracker):
