diff --git a/torch/distributed/distributed_c10d.py b/torch/distributed/distributed_c10d.py
index f14a24db8c1..251f37caad1 100644
--- a/torch/distributed/distributed_c10d.py
+++ b/torch/distributed/distributed_c10d.py
@@ -92,6 +92,7 @@ __all__ = [
     "is_mpi_available",
     "is_backend_available",
     "is_nccl_available",
+    "is_mccl_available",
     "is_torchelastic_launched",
     "is_ucc_available",
     "is_xccl_available",
@@ -141,6 +142,7 @@ _NCCL_AVAILABLE = True
 _GLOO_AVAILABLE = True
 _UCC_AVAILABLE = True
 _XCCL_AVAILABLE = True
+_MCCL_AVAILABLE = True

 try:
     # pyrefly: ignore [import-error, missing-import]
@@ -367,6 +368,8 @@ class Backend(str):  # noqa: SLOT000
             Backend.backend_list.append(name.lower())

         if devices is not None:
+            if isinstance(devices, str):
+                devices = [devices]
             for device in devices:
                 if device not in Backend.default_device_backend_map:
                     Backend.default_device_backend_map[device] = name.lower()
@@ -1278,6 +1280,11 @@ def is_nccl_available() -> bool:
     return _NCCL_AVAILABLE
 
 
+def is_mccl_available() -> bool:
+    """Check if the MCCL backend is available."""
+    return _MCCL_AVAILABLE
+
+
 def is_gloo_available() -> bool:
     """Check if the Gloo backend is available."""
     return _GLOO_AVAILABLE
@@ -2248,7 +2255,7 @@ def _new_process_group_helper(
     pg._set_group_name(group_name)
     pg._set_group_desc(group_desc)
 
-    if device_id and pg._get_backend(device_id).supports_splitting:
+    if device_id:
         eager_backend = pg._get_backend(device_id)
         eager_backend.eager_connect_single_device(device_id)
 
@@ -2954,6 +2961,9 @@ def broadcast(
     """
     group = _group_or_default_group(group)
     group_src = _canonicalize_group_rank(group, src, group_src, return_global=False)
+    if tensor is None or tensor.numel() == 0:
+        warnings.warn("broadcast with an empty tensor is a no-op")
+        return
     _check_single_tensor(tensor, "tensor")
     if _rank_not_in_group(group):
         _warn_not_in_group("broadcast")
